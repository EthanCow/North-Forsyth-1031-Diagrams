---
description: 5-tab markdown — preserve markers, mermaid colors, serve GOAL.md
globs: North_Forsyth_1031_Diagrams.md
alwaysApply: false
---

# Tab content

Tabs: `starthere`, `overview`, `kao`, `hanover`, `strake`, `references` — must match `index.html` `TAB_ORDER`. `starthere` = plain-English primer + acronym legend (audience: deal participant with no §1031 background).

- Preserve `<!-- TAB:id -->` markers
- Keep mermaid classDef colors (kao/han/neutral/asset)
- **No `~` in markdown prose** — use `≈` for approximations (tilde pairs render as strikethrough)
- Mark unsourced claims DRAFT or OPEN
- Strake tab = internal reference for us, not a Strake deliverable
